{
  "name": "root",
  "private": true,
  "scripts": {
    "start:ui": "lerna run --stream --scope rampup-ui start",
    "start:crud-service": "lerna run --stream --scope crud-service start:dev",
    "start:file-upload-service": "lerna run --stream --scope file-upload-service start:dev"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
